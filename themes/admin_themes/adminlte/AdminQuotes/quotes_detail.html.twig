{% extends _layout %}
{% block content_header %}
    <h1>
        Quote
        <small>detail</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li class="active">Quote</li>
    </ol>
{% endblock %}
{% block content %}
    <div class="row">
    <div class="col-md-3"></div>
        <div class="col-md-6">
            <div class="box">
                <div class="box-header with-border">
                    <h3 class="box-title">{{ quotes ? quotes.name : 'Unknown' }}</h3>
                </div>
                <div class="box-body">
                    <table class="table table-bordered">                            
                        {# <tr>
                            <td colspan="2">
                                <center>
                                {% if quotes and quotes.img is not empty %}
                                    <img src="{{ asset('img/quotes/' ~ quotes.img ,true) }}" style="width:200px">
                                {% else %}
                                    <img src="{{ asset('img/quotes/default.png',true) }}" style="width:200px">
                                {% endif %}
                                </center>
                            </td>
                        </tr> #}
                        <tr>
                            <th>Name</th>
                            <td>{{ quotes.name }}</td>
                        </tr>   
                        <tr>
                            <th>Description</th>
                            <td>{{ quotes.description }}</td>
                        </tr>            
                    </table>
                </div>
                <div class="box-footer">
                    <a href="{{ generate_url('admin_quotes_index') }}" class="btn btn-warning"><i class="fa fa-chevron-left"></i> Back</a> 
                    <a href="{{ generate_url('admin_quotes_form') }}?mode=edit&id={{quotes.id}}" class="btn btn-info" style="margin-left:5px;">Edit</a> 
                </div>
            </div>
        </div>
        <div class="col-md-6"></div>
    </div>
{% endblock %}
{% block bottom_resource %}
    {{parent()}}
    <script>
    </script>
{% endblock %}