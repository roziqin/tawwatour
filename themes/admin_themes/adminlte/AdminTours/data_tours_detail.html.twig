{% extends _layout %}
{% block content_header %}
    <h1>
        Tours
        <small>detail</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li class="active">Tours</li>
    </ol>
{% endblock %}
{% block content %}
    <div class="row">
    <div class="col-md-3"></div>
        <div class="col-md-6">
            <div class="box">
                <div class="box-header with-border">
                    <h3 class="box-title">{{ tourss ? tourss.name : 'Unknown' }}</h3>
                </div>
                <div class="box-body">
                    <table class="table table-bordered">                            
                        <tr>
                            <td colspan="2">
                                <center>
                                {% if tourss and tourss.img is not empty %}
                                    <img src="{{ asset('img/tours/' ~ tourss.img ,true) }}" style="width:200px">
                                {% else %}
                                    <img src="{{ asset('img/tours/default.png',true) }}" style="width:200px">
                                {% endif %}
                                </center>
                            </td>
                        </tr>
                        <tr>
                            <th>Caption</th>
                            <td>{{ tourss.name }}</td>
                        </tr>
                        <tr>
                            <th>Category</th>
                            <td>{{ tourss.tour_category.name }}</td>
                        </tr>
                        <tr>
                            <th>Caption</th>
                            <td>{{ tourss.description_summary }}</td>
                        </tr> 
                        <tr>
                            <th>Description</th>
                            <td>{{ tourss.description }}</td>
                        </tr>            
                    </table>
                </div>
                <div class="box-footer">
                    <a href="{{ generate_url('admin_tours') }}" class="btn btn-warning"><i class="fa fa-chevron-left"></i> Back</a> 
                    <a href="{{ generate_url('admin_tours_form') }}?mode=edit&id={{tourss.id}}" class="btn btn-info" style="margin-left:5px;">Edit</a> 
                </div>
            </div>
        </div>
        <div class="col-md-6"></div>
    </div>
{% endblock %}
{% block bottom_resource %}
    {{parent()}}
    <script>
    </script>
{% endblock %}